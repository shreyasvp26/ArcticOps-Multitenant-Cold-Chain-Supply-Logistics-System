export default function Page() {
  return (
    <div style={{ padding: "2rem", color: "var(--ao-text-secondary)", fontFamily: "var(--ao-font-mono)", fontSize: "0.75rem" }}>
      {/* Settings (Ops) — will be implemented in later steps */}
      <span>Settings (Ops)</span>
    </div>
  )
}
