import { DriverLayoutShell } from "@/components/driver/layout-shell"

export default function DriverLayout({ children }: { children: React.ReactNode }) {
  return <DriverLayoutShell>{children}</DriverLayoutShell>
}
